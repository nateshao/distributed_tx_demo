spring:
  datasource:
    url: jdbc:mysql://localhost:4000/test
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8086 